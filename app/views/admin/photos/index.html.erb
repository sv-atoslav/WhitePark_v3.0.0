<p id="notice"><%= notice %></p>
<h1>Photos</h1>
<br><%= link_to 'New Photo', action: "new" %>
<% if @good_photos.first.nil? %>
  <i>эта таблица пустая</i>
<% else %><table class="sv_table">
  <thead><tr>
    <th>Preview</th>
    <th>Description</th>
    <th colspan="3"></th>
  </tr></thead>
<<<<<<< HEAD
  <% puts "good_photos = "+ @good_photos.to_s %>
  <tbody><% @good_photos.each do |photo| %><tr>
    <% unless photo.avatar.exists? %>
      <td align="center"><%= $INE.html_safe %></td>
    <% else %>
      <td><%=   image_tag(photo.avatar.url , style: "width: 100px;", alt: photo.description) %>
        <br><%= image_tag(photo.avatar.path, style: "width: 100px;", alt: photo.description) %>
      </td>
    <%  end %>
    <td><%= photo.avatar_file_name %></td>
    <td><%= photo.description %><!-- [ and
      <% similar = (photo.avatar.path=="/app/public" + photo.avatar.url[0...photo.avatar.url.index("?")])%>
      <%= similar %>]<br>
      <% unless similar %>
        <%= photo.avatar.path %><hr>
        <%= "/app/public" + photo.avatar.url[0...photo.avatar.url.index("?")] %>
      <% end %>
 -->    </td>
    <td><%= link_to 'Show',      admin_photo_path(photo) %></td>
    <td><%= link_to 'Edit', edit_admin_photo_path(photo) %></td>
    <td><%= link_to 'Destroy',   admin_photo_path(photo), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr><%   end %></tbody>
</table><%  end %>
<hr>
<% @bad_photos.each do |one_temp_photo|   %>
  <br><%= one_temp_photo.avatar.file?     %>
  <br><%= one_temp_photo.avatar.present?  %>
  <br><%= one_temp_photo.id %>
  <br><%= one_temp_photo.avatar.to_s %>
  <br><%= one_temp_photo.avatar.path %>
  <br><%= image_tag(one_temp_photo.avatar.url ) %>
  <% if !one_temp_photo.avatar.path.nil? %>
    <br><%= image_tag(one_temp_photo.avatar.path) %>
  <% end %>
  <br><%= link_to 'Show',   admin_photo_path(one_temp_photo) %>
  <%= link_to 'Edit',  edit_admin_photo_path(one_temp_photo) %>
  <%= link_to 'Destroy',    admin_photo_path(one_temp_photo), method: :delete, data: { confirm: 'Are you sure?' } %>
  <hr>
<% end %>
=======
  <tbody><% @photos.each do |photo| %><tr>
    <% unless photo.avatar? %>
      <td colspan="2" align="center"><%= $INE.html_safe %></td>
    <% else %>
      <td><%= image_tag(photo.avatar.url , style: "width: 100px;", alt: photo.description) %></td>
    <% end %>
    <td><%= photo.description %></td>
    <td><%= link_to 'Show',      admin_photo_path(photo) %></td>
    <td><%= link_to 'Edit', edit_admin_photo_path(photo) %></td>
    <td><%= link_to 'Destroy',   admin_photo_path(photo), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr><% end %></tbody>
</table><% end %>
<br><%= link_to 'New Photo', action: "new" %>
>>>>>>> work_with_paper_clip

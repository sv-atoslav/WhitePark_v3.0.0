<p id="notice"><%= notice %></p>
<%= link_to 'Edit', action: "edit" %> | 
<%= link_to 'Back', action: "index" %>
<p><strong>Description:</strong><%= @photo.description %></p>
<% unless @photo.avatar? %><%= $INE.html_safe %>
<% else %>
	<!-- <strong>Title:</strong><%# @photo.avatar %><%# des = @photo.description %> -->
	<%= image_tag(@photo.avatar.url, width: "100%", alt: @photo.description) %>
<% end %>

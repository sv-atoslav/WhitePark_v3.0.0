<p id="notice"><%= notice %></p>
<% unless @photo.avatar.exists? %><%= $INE.html_safe %>
<% else %>
	<strong>Title:</strong><%= @photo.avatar.name %><% des = @photo.description %>
	<%= image_tag(@photo.avatar.url, width: "100%", alt: @photo.description) %>
<% end %>
<p><strong>Description:</strong><%= @photo.description %></p>
<%= link_to 'Edit', action: "edit" %> | 
<%= link_to 'Back', action: "index" %>

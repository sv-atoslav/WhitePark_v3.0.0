<div class="middle-box text-center loginscreen  animated fadeInDown"><div>
    <div><h1 class="logo-name">IN+</h1></div>
    <h3>Welcome to IN+</h3>
    <p>Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.</p>
    <p>Login in. To see it in action.</p>
    <%= form_tag('/admin', method: :get, class: "m-t") do %>
        <div class="form-group"><%= text_field_tag("admin_name", "Username", class: "form-control" )%></div>
        <div class="form-group"><%= password_field_tag("admin_password", "Password", class: "form-control" )%></div>
        <button type="submit" class="btn btn-primary block full-width m-b">Login</button>
    <% end %>
    <p class="m-t"><small>Inspinia we app framework base on Bootstrap 3 &copy; 2015</small> </p>
</div></div>

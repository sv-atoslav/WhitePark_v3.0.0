<% if @visible_part %><div class="photoreports-block  big-block"><div class="block">
	<h2>События</h2>
	<hr class="spec-hr">
	<div class="about-us-image-set"><% @event_list.each do |one_event| %>
		<a href = "//localhost:3000/events/<%= one_event.id.to_s %><%= $WORD_TO_SEE %>" class = "album">
			<div class="album-bg">
				<% temp_icon = Photo.find_by(id: one_event.icon) %>
				<% if temp_icon.nil? %><%= $INE.html_safe %>	
				<% else %><p><%= image_tag temp_icon.avatar.url %></p>
				<% end  %>
			</div>
			<div class="album-title"><p><%= one_event.ru_title %></p><h4>подробнее</h4></div>
		</a>
	<% end %></div>
</div></div><% end %>
